apiVersion: v1
kind: Service
metadata:
  name: reservation-redis
  labels:
    app: reservation-redis
spec:
  selector:
    app: reservation-redis
  ports:
  - name: redis
    port: 6379
    targetPort: 6379
  type: ClusterIP
