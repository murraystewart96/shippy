apiVersion: v1
kind: Service
metadata:
  name: reservation-service
  labels:
    app: reservation-service
spec:
  selector:
    app: reservation-service
  ports:
  - name: grpc
    port: 50051
    targetPort: 50051
  type: ClusterIP
