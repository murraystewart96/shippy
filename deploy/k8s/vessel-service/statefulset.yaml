apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: vessel-service-database
spec:
  selector:
    matchLabels:
      app: vessel-service-database
  serviceName: vessel-service-database
  replicas: 1
  minReadySeconds: 0
  template:
    metadata:
      labels:
        app: vessel-service-database
    spec:
      terminationGracePeriodSeconds: 10
      containers:
      - name: vessel-service-database
        image: mongo
        ports:
        - containerPort: 27017
          name: mongodb
        volumeMounts:
        - name: vessel-db-data
          mountPath: /data/db
  volumeClaimTemplates:
  - metadata:
      name: vessel-db-data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 1Gi
