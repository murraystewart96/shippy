apiVersion: v1
kind: Service
metadata:
  name: vessel-service-database
  labels:
    app: vessel-service-database
spec:
  selector:
    app: vessel-service-database
  type: ClusterIP
  clusterIP: None
  publishNotReadyAddresses: true
  ports:
    - name: mongodb
      port: 27017
      targetPort: 27017
      protocol: TCP
